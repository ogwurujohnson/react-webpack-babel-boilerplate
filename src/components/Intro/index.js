import React from 'react'

const Intro = props => {
  return (
    <div>
      <p>Welcome to My First React, Webpack and Babel Boilerplate. version - {props.version}</p>
    </div>
  )
}

export default Intro;
